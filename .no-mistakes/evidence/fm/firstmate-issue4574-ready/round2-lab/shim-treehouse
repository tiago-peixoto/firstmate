#!/bin/bash
# Stand-in for 'treehouse get': enter the next prepared isolated worktree.
if [ "${1:-}" = get ]; then cd "$(cat "/var/folders/r8/cylyt7xd7t50y9x5wc05my380000gn/T//fm4574-live/next-wt")" && exec /bin/bash --noprofile --norc -i; fi
exit 0
