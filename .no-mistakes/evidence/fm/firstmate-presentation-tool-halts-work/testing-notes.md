# Presentation-only prerequisites: testing evidence

The real `bin/fm-bootstrap.sh`, `bin/fm-session-start.sh`, and `bin/fm-fleet-view.sh` were executed against disposable Git homes. External tool availability/version responses and harness process identity were fixtures; no live AI worker, installation, GitHub mutation, or visual Lavish session was started. The queued code/report entries in the fleet reports are fixture inputs, not claims of completed worker tasks.

## Commands and observations

1. `TMPDIR="$PWD/.test-tmp" bash tests/fm-bootstrap.test.sh` exercised the targeted bootstrap test file. It encountered inherited Git commit signing at the routine-bootstrap fixture: `gpg: skipped "fmtest <fmtest@example.invalid>": No secret key`, then `routine bootstrap confirmations should be silent, got: SECONDMATE_SYNC: secondmate sm: skipped: primary default-branch commit cannot be resolved`.
2. `TMPDIR="$PWD/.test-tmp" GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false bash tests/fm-bootstrap.test.sh` passed. This changed only the subprocess environment, not source, fixtures, shared settings, or Git configuration files. The previously declined composer/Muse baseline failures were not revisited.
3. `TMPDIR="$PWD/.test-tmp" GIT_CONFIG_COUNT=1 GIT_CONFIG_KEY_0=commit.gpgsign GIT_CONFIG_VALUE_0=false bash .test-tmp/check-presentation.sh` ran the temporary manual verification driver. Its initial completion-PID assertion used a Claude fixture whose contiguous harness ancestry included the outer Pi process; switching the temporary identity fixture to Codex isolated the intended holder. The final run passed. Temporary driver/fixtures and abandoned workers from the setup attempts were cleaned up.
4. The driver invoked `bin/fm-bootstrap.sh` with Lavish absent, at `0.1.45`, `0.1.46`, and `0.1.47`, with an unparseable version, and with a failed version command. A call log verified exactly one `--version` invocation for installed Lavish fixtures; compatible versions were silent, while unavailable versions retained the explicit `>=0.1.46` requirement and sanctioned plain-text fallback.
5. For absent and below-floor Lavish, `bin/fm-session-start.sh` completed with a verified writable session lock, preserved the diagnostic in its emitted digest, and persisted `.session-start-complete` with the fixture holder PID. `bin/fm-startup-network.sh wait 15` waited for deferred checks; `bin/fm-fleet-view.sh` then generated the attached plain-text Markdown reports without Lavish.
6. With both `gh-axi` and Lavish absent, bootstrap emitted both `MISSING: gh-axi` and `PRESENTATION_UNAVAILABLE: lavish-axi`. Bootstrap is a diagnostic interface that exits zero for dependency findings; blocking versus optional availability is conveyed by the diagnostic and agent guidance, not its exit code.
7. `git show 891dc51744f52f8f5b12b212e174efbfcf6de491:bin/fm-bootstrap.sh` supplied a temporary executable paired with unchanged helper libraries. Executing it with absent and below-floor Lavish reproduced the old blocking `MISSING: lavish-axi` classification.

The attached transcripts are actual CLI output, not source-text assertions. No screenshot was needed: this change affects CLI diagnostics and agent guidance, not a rendered application UI. No full repository suite, lint, formatting, static analysis, or other pipeline phase was run.
