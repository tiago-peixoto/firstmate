The 220-character status-line cap must bound the line that is actually
APPENDED - stamp included. Driven through the real bin/fm-send.sh into an
isolated home, answering an open decision with a 400-character note.

==================================================================
8. fm-send --resolve-key with an over-long answer
==================================================================
$ bin/fm-send.sh t2 --resolve-key rollout-plan \
    'stage it behind a flag for a week then ramp: xxxx...(400 x)'

--- previous branch head ce3b5c2 (cap measured BEFORE the stamp) ---
  stored line length: 236          <- 16 characters past the cap fm-send's own
                                      refusal message cites
--- target 91d9b79 (stamp width reserved) ---
  stored line length: 220

$ tail -1 state/t2.status          # target
resolved [key=rollout-plan] [at=1789880404]: answered: stage it behind a flag for a week then ramp: xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx [truncated]

$ bin/fm-wake-drain.sh
(no OPEN DECISIONS section)        -> the capped, stamped close still closes
                                      rollout-plan; truncation did not eat the
                                      structural [key=...] delimiter.

==================================================================
9. The generated ship brief, rendered by the real bin/fm-brief.sh
==================================================================
$ bin/fm-brief.sh t7 demo --mode no-mistakes
scaffolded: .../data/t7/brief.md (ship, mode=no-mistakes; ...)

4. Report status by appending one line:
   `echo "{state} [at=<epoch>]: {one short line}" >> '.../state/t7.status'`
   States: working, needs-decision, blocked, paused, done, failed.
   Substitute `<epoch>` with the current Unix time in seconds - run `date +%s`
   and write the number it printed; a stamp that is not plain digits records no
   time at all.

...and every other reporting instruction in the same brief now carries it:
  20:If the top-level path is the primary checkout or not the worktree you were launched in, STOP - do not branch or commit here - append `blocked [at=<epoch>]: launched in primary checkout, not an isolated worktree` to the status file and stop.
  30:   `echo "{state} [at=<epoch>]: {one short line}" >> '/tmp/fm-live-stamp.hOalKI/home-brief/state/t7.status'`
  46:5. If you hit the same obstacle twice, append `blocked [at=<epoch>]: {why}` and stop; firstmate will help.
  48:   append `needs-decision [at=<epoch>]: {summary of options}` and stop. Firstmate will reply with the decision.
  50:   `needs-decision [at=<epoch>] [key=nm-<run>-<step>]: ask-user findings=<id1>,<id2>,... file=/tmp/fm-live-stamp.hOalKI/home-brief/data/t7/nm-<run>-findings.txt`
  53:   Firstmate's reply normally writes that closing line at answer time; when a blocker or wait clears WITHOUT a firstmate reply, append `resolved [at=<epoch>]: {how it cleared}` yourself (same `[key=<slug>]` if you opened it with one) as you resume.
  59:   `blocked [at=<epoch>]: {the daemon error}` and stop even when the local run record still says running or
  82:When you believe it is complete, append `done [at=<epoch>]: {summary}` to the status file and stop.
  110:After /no-mistakes reports CI green (the CI-ready return point - do not wait for it to keep monitoring in the background until merge), append `done [at=<epoch>]: PR {url} checks green` and stop. You are finished.

The round trip in section 2 above is that instruction executed verbatim in a
real worker pane, read back through bin/fm-fleet-snapshot.sh.
