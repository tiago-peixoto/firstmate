Targeted suites for this change, and proof the new suites are wired into a CI lane
when: 2026-09-12T06:20:24Z

$ bash tests/fm-pr-state.test.sh
ok - a passing required check and a skipped one leave nothing to report
ok - a terminal pull request reports that state and nothing else
ok - draft state blocks readiness
ok - stale changes-requested verdicts explain a blocking review decision
ok - approved PR ignores stale changes-requested history
ok - current changes-requested review blocks readiness
ok - a CHANGES_REQUESTED decision is always reported
ok - the author's own review is never listed as a blocker
ok - a pending approval is not reported as a blocker
ok - required failure blocks readiness
ok - an unreported required check is unconfirmed, other check lookup failures refuse
ok - a head with no reported checks is unverified rather than ready
ok - help states what empty output means and what is out of scope
ok - unknown and conflicting mergeability block readiness
ok - argument and lookup refusals exit nonzero
(exit 0)

$ bash tests/fm-pr-reviewers.test.sh
ok - reviewer candidates use API-resolved logins and unique commits
ok - author-only evidence produces no candidate and says why
ok - argument and lookup refusals exit nonzero
(exit 0)

$ FM_PR_STATE_LIVE_E2E=1 bash tests/fm-pr-state-live-e2e.test.sh   (runs the real gh, real GitHub, gh own gojq engine)
ok - fm-pr-state.sh's pull-request read programs are accepted by gh's jq engine
(exit 0)

$ bin/fm-test-run.sh --list --family pr-forge
tests/fm-check-unregister.test.sh
tests/fm-pr-check-security.test.sh
tests/fm-pr-merge.test.sh
tests/fm-pr-reviewers.test.sh
tests/fm-pr-state.test.sh
tests/fm-review-diff.test.sh
tests/fm-teardown.test.sh
tests/fm-x-mode.test.sh

$ bin/fm-test-run.sh --list --lane portable-serial | grep fm-pr-
tests/fm-pr-reviewers.test.sh
tests/fm-pr-state-live-e2e.test.sh
tests/fm-pr-state.test.sh

$ bin/fm-test-run.sh --check-coverage
FM_TEST_COVERAGE ok total=202 parallel=24 parallel_max_ms=417163 parallel_imbalance_ms=2894 parallel_unhinted=0 serial=162 serial_shards=5 serial_unhinted=16 herdr=16
(exit 0)
